# Frontend Dockerfile for Agent Foundry UI (Next.js)
# Development mode with hot reload

FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package.json package-lock.json* ./
RUN npm ci

# Copy app files (will be overridden by volume mount in dev)
COPY . .

# Expose Next.js dev server port
EXPOSE 3000

# Run development server with hot reload
CMD ["npm", "run", "dev"]
